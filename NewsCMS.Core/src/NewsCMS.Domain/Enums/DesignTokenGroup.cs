namespace NewsCMS.Domain.Enums;

/// <summary>Nhóm design token — mỗi nhóm sinh ra một phần của khối @theme Tailwind + CSS variables.</summary>
public enum DesignTokenGroup
{
    Color = 0,
    Font = 1,
    Space = 2,
    Radius = 3,
    Shadow = 4
}
