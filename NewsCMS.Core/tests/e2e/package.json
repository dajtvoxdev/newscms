{
  "name": "newscms-e2e",
  "private": true,
  "description": "Test hồi quy trình duyệt cho Site Builder (Phase 1.2 — chống tái phát bug lệch realm 0.1).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
